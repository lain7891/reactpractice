import React from 'react';

const Math = (props) => {const result = props.num1 + props.num2;

    
    return (
        <span>

            {results}
        </span>
        
        
    );
};

export default Math;